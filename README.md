ember-table-scroll-issue
========================

`bower install`

open index.html

Scroll around with a trackpad. Content should scroll vertically just fine, but not horizontally. Grabbing the scrollbar and dragging does work, however.
